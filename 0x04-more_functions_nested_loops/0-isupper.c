#include "main."

/**
 * _isupper(int c),function to check the upper case letters
 *
 * return (1) if the letter is upper case
 *
 * retur (0).if the letter is not upper case
 */
 int _isupper(int c)

{ 		
	char C;

	if((C >='A') && (C<='Z'));

          	return(1); 

	else 
	
	          return(0);
	
	}
